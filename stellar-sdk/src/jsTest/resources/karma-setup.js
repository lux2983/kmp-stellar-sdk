// Karma test setup — loaded before test files via karma.conf.js
// libsodium is bundled by webpack, so no manual script loading is needed.
// This file exists as a hook for any browser-specific test setup.

(function() {
    console.log('✓ Karma test setup loaded');
})();

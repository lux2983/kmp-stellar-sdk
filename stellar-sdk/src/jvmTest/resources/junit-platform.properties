# Configure JUnit 5 to run tests in alphabetical method name order.
# This ensures integration tests with sequential dependencies
# (e.g., upload → create → invoke) execute in the correct order.
junit.jupiter.testmethod.order.default=org.junit.jupiter.api.MethodOrderer$MethodName
